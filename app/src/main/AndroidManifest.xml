<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.tez.com.takes">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.Dark" >
        <activity
            android:name=".block.anasayfa.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".block.anasayfa.KayitOlEkrani" android:theme="@style/AppTheme.Dark"/>

        <service android:name=".block.TCPSERVERCLIENT.KayitEkraniService"/>
        <service android:name=".block.TCPSERVERCLIENT.DosyaGonderService"/>
        <service
            android:name=".block.TCPSERVERCLIENT.ServerService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="app.tez.com.takes.LONGRUNSERVICE" />
            </intent-filter>
        </service>
    </application>

</manifest>